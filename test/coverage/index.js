const { expect } = require('chai');

require('../../resources/coverable');

describe('test/coverage', function () {
    it('should always be true', function(){
        expect(true).to.be.ok;
    });
});
